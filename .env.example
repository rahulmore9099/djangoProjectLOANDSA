# Example environment variables for this project
# Copy this to a file named .env and customize before deploying

# Secret key for Django - set this in production
SECRET_KEY=change-me

# DEBUG mode: set to False in production
DEBUG=True

# Comma-separated allowed hosts for production (e.g. example.com,www.example.com)
ALLOWED_HOSTS=localhost,127.0.0.1
